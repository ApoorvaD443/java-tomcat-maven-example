apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-tomcat-app
  labels:
    app: java-tomcat-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: java-tomcat-app
  template:
    metadata:
      labels:
        app: java-tomcat-app
    spec:
      containers:
        - name: java-tomcat-app
          image: apoorvad4/tomcat_maven:latest   
          ports:
            - containerPort: 8080
          imagePullPolicy: Always
